class MirrorFS {
    constructor() {}

    static async init(config) {
        //TODO-MIRROR API unsure what config looks like
        this.config = config;   // Save config
    }

}
exports = module.exports = MirrorFS;