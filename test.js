
//HashStore = require('./HashStore');

//TODO Add tests from each of the classes when/if they exist

process.env.DEBUG="dweb-transports dweb-transports:* dweb-objects dweb-objects:*";  // Get highest level debugging of these two libraries, must be before require(dweb-transports) //TODO-MIRROR check using GUN for metadata
